on:
  issues:
    types:
      - opened
      - reopened

jobs:
  notion:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/github-script@v7
        with:
          script: |
            console.log("${{ github.event.issue.assignee }}");
            console.log("${{ github.event.issue.assignees }}");
            console.log("${{ github.event.issue.labels }}");
