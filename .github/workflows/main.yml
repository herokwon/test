on:
  issues:
    types:
      - opened
      - reopened

jobs:
  notion:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/github-script@v7
        with:
          script: |
            console.log("${{ fromJSON(github.event.issue.assignees)[0].name }}");
            console.log("${{ github.event.issue.labels[1].name }}");
